# API layer

This directory is reserved for future server endpoints and route handlers.

Planned examples:
- `/api/projects`
- `/api/testimonials`

Frontend code should not rely on GitHub API secrets directly. Keep the public-facing UI decoupled from provider-specific behavior.
