# Data

Place static public data, fallback content, and local fixtures here.

This directory is intentionally separate from page logic and service implementations so future content sources can be swapped without changing component code.
