# Services

This directory is reserved for provider-specific integrations and future API abstractions.

Planned examples:
- GitHub project discovery
- testimonial moderation and approval checks
- server-side data adapters

Keep provider-specific logic isolated here so UI components can consume a stable interface.
